/**
 * Seed Admin Command
 *
 * Command to seed the initial admin user from environment config.
 * Idempotent - does nothing if admin already exists.
 */
export class SeedAdminCommand {
  constructor() {}
}
