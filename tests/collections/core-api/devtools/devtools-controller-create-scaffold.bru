meta {
  name: devtools-controller-create-scaffold
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/__arch/scaffold
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

assert {
  res.status: eq 201
}
