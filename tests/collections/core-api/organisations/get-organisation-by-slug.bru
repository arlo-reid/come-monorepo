meta {
  name: Get organisation by slug
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/organisations/{{orgSlug}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.id: isDefined
  res.body.name: isDefined
  res.body.slug: isDefined
  res.body.ownerId: isDefined
  res.body.createdAt: isDefined
  res.body.updatedAt: isDefined
}

docs {
  Get a single organisation by its URL-friendly slug identifier.
  
  Path parameters:
  - slug: The URL-friendly identifier of the organisation
}
