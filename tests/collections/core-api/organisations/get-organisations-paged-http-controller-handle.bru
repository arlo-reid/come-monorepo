meta {
  name: Get paginated list of organisations
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/organisations
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

assert {
  res.status: eq 200
}
